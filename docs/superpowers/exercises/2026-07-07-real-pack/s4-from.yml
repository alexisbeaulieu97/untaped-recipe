version: 1
inputs:
  repo_name:
    type: str
    required: true
    from:
      - "{{ record.repo }}"
      - "{{ target.name }}"
steps:
  - type: template
    template: templates/owner.yml
    dest: .owner.yml
    if_absent: true
