[project]
name = "untaped-recipe-hook-api"
version = "0.8.0"
description = "Typed hook authoring contract for untaped-recipe projects."
readme = "README.md"
authors = [
    { name = "Alexis Beaulieu", email = "alexisbeaulieu97@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[build-system]
requires = ["uv_build>=0.11.8,<0.12.0"]
build-backend = "uv_build"
